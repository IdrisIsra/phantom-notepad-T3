# Phantom Notepad T3

This app uses the follow from the T3 stack:

- Typescript
- tRPC with websockets
- Tailwind

Also uses Redis.

Do the following:

```
npm install
docker-compose up -d
npm run dev
```
